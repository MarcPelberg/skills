interface:
  display_name: "Reset Agentic Context"
  short_description: "Reset AI context and tooling safely"
  default_prompt: "Use $reset-agentic-context to perform my full recoverable clean-slate reset across every repository and registered worktree plus my Codex and Claude global settings; audit the visible skill registry, name any plugin that requires exact removal approval, skip unsafe items, journal everything, and never call residual context clean."

policy:
  allow_implicit_invocation: false
